# requirements.txt for DNS Security Blocklist Builder v9.1.0

# Core dependencies
aiohttp>=3.11.16,<4.0.0  # Fixed CVE-2025-69223, CVE-2025-69224-69229
aiofiles>=24.1.0

# Optional: for better performance
# uvloop>=0.21.0  # Unix only, faster event loop

# Development dependencies (for testing)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
black>=24.0.0
ruff>=0.3.0
mypy>=1.8.0
